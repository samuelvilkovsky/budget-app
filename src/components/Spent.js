import React from "react";

// created new component
const Spent = () => {
    return(
        <div className="alert alert-primary">
            <span>Spent: 1000</span>
        </div>
    );
};

export default Spent;